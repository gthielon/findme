{% extends 'base.html.twig' %}

{% block body %}
    <h4>Ajouter une proposition</h4>
    <div class="row">
        <div class="col offset-s1 s10">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="row">
                <div class="col offset-s3 s4">
                    <a class="waves-effect waves-light btn btnStart" }}"><input type="submit" value="Valider" /></a>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
