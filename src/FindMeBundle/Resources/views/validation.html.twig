
{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col offset-s1 s10">
            <div class="row">
                <h4>
                    Validation
                </h4>
            </div>
            <div class="row contenu">
                <div class="col offset-s1 s10">
                    <p>
                        Proposition de : {{ app.user.username }}
                    </p>
                    <div class="row">
                        <div class="col s12 m7">
                            <div class="card">
                                <div class="card-image">
                                    <img class="media image-right right" src="{{ asset(game.photo.author_id.user.username) }}" alt=""/>
                                    <span class="card-title">Card Title</span>
                                </div>
                                <div class="card-content">
                                    <p>I am a very simple card. I am good at containing small bits of information.
                                        I am convenient because I require little markup to use effectively.</p>
                                </div>
                                <div class="card-action">
                                    <a href="#">Valider l'indice</a>
                                    <a href="#">Valider l'indice</a>

                                </div>
                            </div>
                        </div>
                    </div>

                    <a class="waves-effect waves-light btn" href="{{ path('game_index') }}">Valider l'indice</a>
                    <a class="waves-effect waves-light btn" href="{{ path('find_me_homepage') }}">Valider l'indice</a>
                </div>
            </div>
        </div>
    </div>

    {{ parent() }}
{% endblock body %}
