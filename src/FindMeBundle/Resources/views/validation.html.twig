
{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col offset-s1 s10">
            <div class="row">
                <h4>
                    Validation
                </h4>
            </div>
            <div class="row contenu">
                <div class="col offset-s1 s10">
                    <p>
                        Proposition de : {{ app.user.username }}
                    </p>
                    {#
                                Rôle : Meneur
                    #}

                    <a class="waves-effect waves-light btn" href="{{ path('game_index') }}">Gagné, rejoignez une nouvelle partie</a>
                    <a class="waves-effect waves-light btn" href="{{ path('find_me_homepage') }}">Perdu, attendez une nouvelle proposition</a>
                </div>
            </div>
        </div>
    </div>

    {{ parent() }}
{% endblock body %}
